#include <iostream>
using namespace std;

// We use endl and '\n' for new line and to break the line.
// endl and '\n' is basically used in place of enter

// Data-Types
// 1-> Int
// 2-> Char
// 3-> Bool
// 4-> Float
// 5-> Double

int main()
{
    // => Data Types

    // int a = 123;
    // cout << a << endl;

    // char b = 'a';
    // cout << b << endl;

    // bool c = false;
    // cout << c << endl;

    // float f = 1.2;
    // cout << f << endl;

    // double d = 1.23;
    // cout << d << endl;

    // cout <<sizeof(a)<<endl;

    // Type Casting  =>Type Casting changes the type of data
    // 1 Byte = 8 Bits

    // int a = 'a';
    // cout << a << endl;
    // char ch = 99;
    // cout << ch << endl;
    // return 0;

    // => Signed and Unsigned Values
    // Signed values means we store both positive and negative integer in int data type.
    // Unsigned values means we store only positive integer in int data type.
    // Note: If we store negative value in unsigned its output is huge.

    // unsigned int a = -18;
    // cout << a << endl;

    // 1=> Operators
    // Arithemetic Operators

    // + , - , * , / , %
    // int a = 12;
    // int b = 7;
    // float c = static_cast<float>(a) / b;
    // int c = a / b;
    // cout << c << endl;

    // 2=> Relational Operators
    // = , == , === , < , > , >= , <= , !=
    // int a = 12;
    // int b = 13;

    // bool first = (a == b);
    // cout << first << endl;

    // bool second = (a < b);
    // cout << second << endl;

    // bool third = (a > b);
    // cout << third << endl;

    // bool fourth = (a >= b);
    // cout << fourth << endl;

    // bool fifth = (a <= b);
    // cout << fifth << endl;

    // bool sixth = (a != b);
    // cout << sixth << endl;

    // Logical Operators
    // && , || , !
}
